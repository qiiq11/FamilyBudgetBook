# 云平台公网部署指南

> **已部署上线**：http://123.60.7.243
> 华为云 ECS + Node.js 22 + PM2 + Nginx

---

## 最终部署方案

### 部署环境

| 项目 | 说明 |
|------|------|
| 云服务商 | 华为云弹性云服务器 ECS |
| 配置 | 1vCPU / 1GB / 40GB SSD |
| 操作系统 | Ubuntu 24.04 LTS |
| 公网 IP | 123.60.7.243 |
| Web 服务器 | Nginx 1.24 |
| 进程管理 | PM2 |
| 运行时 | Node.js 22.22.2 |

### 部署步骤

1. 购买华为云 ECS（Ubuntu 24.04），安全组放行 80 端口
2. 安装 Node.js 22：
   ```bash
   curl -fsSL https://deb.nodesource.com/setup_22.x | bash -
   apt install -y nodejs nginx
   ```
3. 克隆项目：
   ```bash
   git clone https://github.com/qiiq11/FamilyBudgetBook.git
   cd FamilyBudgetBook
   ```
4. 构建前端：
   ```bash
   cd frontend && npm install && npm run build
   ```
5. 安装后端依赖并创建 .env：
   ```bash
   cd ../backend && npm install --omit=dev
   # .env 内容：NODE_ENV=production, PORT=3000, HOST=0.0.0.0, JWT_SECRET=xxx, DB_PATH=./data/family_budget.db
   ```
6. 配置 Nginx（80 → 前端静态文件，/api/ → 后端 3000 端口）
7. PM2 启动并设置开机自启：
   ```bash
   npm install -g pm2
   pm2 start src/index.js --name family-budget
   pm2 save && pm2 startup
   ```
8. 生成演示数据：
   ```bash
   node src/seed.js
   ```

### 公网验证

- 首页：http://123.60.7.243
- API 健康检查：http://123.60.7.243/api/health
- 演示账号：demo / 123456

---

## 答辩现场检查清单

- [ ] 使用 http://123.60.7.243 打开，不是 localhost
- [ ] 手机流量能打开
- [ ] 演示账号能登录，首页有统计数据与图表
- [ ] 收支记账（TransactionsView）演示 1 次
- [ ] 统计页（日/周/月/年切换）演示 1 次
- [ ] 服务器已 PM2 设置开机自启
- [ ] `00-项目访问地址.txt` 已填写公网地址

---

## 常见问题

**Q：访问公网 IP 打不开？**
检查华为云安全组是否放行 80 端口；`pm2 status` 检查后端状态。

**Q：能开首页但登录失败？**
在服务器执行：`cd /root/FamilyBudgetBook/backend && node src/seed.js`

**Q：重启服务器后数据没了？**
PM2 开机自启 + DB_PATH 指向固定目录，数据持久化。

